name: Deploy Frontend

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build application
      run: npm run build

    - name: Wait for backend deployment
      run: sleep 30

    - name: Deploy to server
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USER }}
        key: ${{ secrets.SSH_KEY }}
        port: ${{ secrets.SSH_PORT || 50022 }}
        script: |
          # Create deployment directory if it doesn't exist
          sudo mkdir -p /var/www/weekilaw-frontend

          # Add maintenance mode or backup (if needed)
          echo "Starting frontend deployment..."

    - name: Sync built files to server
      uses: burnett01/rsync-deployments@6.0.0
      with:
        switches: -avz --delete --exclude='.git' --timeout=300
        path: dist/
        remote_path: /var/www/weekilaw-frontend/
        remote_host: ${{ secrets.SSH_HOST }}
        remote_user: ${{ secrets.SSH_USER }}
        remote_key: ${{ secrets.SSH_KEY }}
        remote_port: ${{ secrets.SSH_PORT || 50022 }}

    - name: Set permissions on server
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USER }}
        key: ${{ secrets.SSH_KEY }}
        port: ${{ secrets.SSH_PORT || 50022 }}
        script: |
          # Set proper permissions
          sudo chown -R www-data:www-data /var/www/weekilaw-frontend

          # Remove any conflicting files that might have been copied from backend
          sudo rm -f /var/www/weekilaw-frontend/index.php
          sudo rm -f /var/www/weekilaw-frontend/.htaccess

          # Restart nginx to ensure changes are picked up
          sudo systemctl reload nginx

          echo "Frontend deployment completed successfully!"
